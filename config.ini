# AutoAimer Configuration File
# Edit paths and thresholds as needed.

# Path to the TensorRT engine file
engine_path = D:/Autoaim/models/head_yolov8s.engine

# Detection thresholds
cls_threshold = 0.35
nms_threshold = 0.45

# Control: aim offset from bbox top center, in pixels (0 = exact top, positive = lower)
aim_offset_y = 10
